vertex_num = getGlobalVertexNumber(); 

for(i=0; i<num_atoms; i++){
    Total_ElectroStaticPotential[vertex_num]+= electrostatic potential contribution 
                                               from interaction between vertex[vertex_num] 
                                               and atom[i]
}
